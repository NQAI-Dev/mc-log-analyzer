[project]
name = "mc-log-analyzer"
version = "0.1.0"
description = "Minecraft log and crash report analyzer"
readme = "README.md"
authors = [
    { name = "NQAI-Dev", email = "nqao-dev@mail.ru" }
]
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
