[project]
name = "mc-log-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NQAI-Dev", email = "nqao-dev@mail.ru" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
mc-log-analyzer = "mc_log_analyzer:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
